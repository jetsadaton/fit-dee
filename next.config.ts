import type { NextConfig } from 'next';

const config: NextConfig = {
  reactStrictMode: true,
  // typedRoutes: true — re-enable in Phase 1 once /me page exists (TabBar pushes /me)
};

export default config;
